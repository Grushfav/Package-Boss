/** Max weight clerks may enter at receive (above tier table = custom quote). */
export const MAX_RECEIVE_LBS = 750

/** Published tier rates apply up to this billable weight. */
export const MAX_AUTO_RATE_LBS = 50
